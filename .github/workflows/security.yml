name: Security (SBOM + Dependency-Check)

on:
  push:
    branches:
      - "**"
  pull_request:

jobs:
  security:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: "20"
          cache: "npm"

      - name: Install dependencies
        run: npm ci

      - name: Generate SBOM (CycloneDX)
        run: |
          npx @cyclonedx/cyclonedx-npm@4.1.2 \
            --output-file sbom.cdx.json \
            --output-format json

      - name: Prepare directories
        run: |
          DATA_DIR="odc-data"
          REPORT_DIR="dependency-check-report"
          mkdir -p "$DATA_DIR" "$REPORT_DIR"
          echo "DATA_DIR=$DATA_DIR" >> $GITHUB_ENV
          echo "REPORT_DIR=$REPORT_DIR" >> $GITHUB_ENV

      - name: Cache Dependency-Check database
        uses: actions/cache@v4
        with:
          path: odc-data
          key: odc-db-${{ runner.os }}-v1
          restore-keys: |
            odc-db-${{ runner.os }}-

      - name: Pull Dependency-Check image
        run: docker pull owasp/dependency-check:latest

      - name: Dependency-Check DB update (heartbeat every 3 min)
        env:
          NVD_API_KEY: ${{ secrets.NVD_API_KEY }}
        run: |
          set -euo pipefail

          echo "=== Pre-check: data dir ==="
          ls -la "${DATA_DIR}" || true
          du -sh "${DATA_DIR}" || true

          # Запускаем docker run и сохраняем ВЕСЬ вывод в файл (чтобы видеть ошибки)
          UPDATE_LOG="docker-update.log"
          rm -f "${UPDATE_LOG}"

          (
            docker run --rm \
              -e "NVD_API_KEY=${NVD_API_KEY}" \
              -v "${PWD}/${DATA_DIR}:/usr/share/dependency-check/data" \
              owasp/dependency-check:latest \
              --updateonly \
              --data /usr/share/dependency-check/data \
              --nvdApiKey "${NVD_API_KEY}" \
              --log /usr/share/dependency-check/data/update.log
          ) >"${UPDATE_LOG}" 2>&1 &

          DC_PID=$!

          # Heartbeat: каждые 3 минуты показываем размер и хвост docker-лога
          while kill -0 "$DC_PID" 2>/dev/null; do
            echo "---- heartbeat ($(date)) ----"
            if [ -f "${DATA_DIR}/odc.mv.db" ]; then
              ls -lh "${DATA_DIR}/odc.m_
